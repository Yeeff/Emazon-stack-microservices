server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost/stock_db
    username: ${username}
    password: ${password}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect